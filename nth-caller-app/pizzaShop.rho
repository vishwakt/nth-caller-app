new stdout(`rho:io:stdout`) in {
    contract @"pizzaShop"(order) = {
        stdout!("Pizza order received: " ++ *order)
    }
    |
    @"pizzaShop"!("Cheese Pizza ")
    |
    @"pizzaShop"!("Cheesy Pizza ")
}
